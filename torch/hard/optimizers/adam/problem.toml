id = "adam"
legacy_ids = ["v1-36"]
title = "Implement Adam from Scratch"
description = "Implement the Adam optimizer from scratch on top of torch.optim.Optimizer: exponential moving averages of the gradient and its square, with bias correction."
difficulty = "hard"
frequency = "common"   # very-likely | common | emerging (derived from company reports)
number = 36
set = "v1"
tracks = ["advanced"]

# GENERATED from the grader spec — edit python/torchleet/problems/*.py, then run: generate.py hints
hints = [
  "Subclass torch.optim.Optimizer and keep per-parameter state in self.state[p] — it starts as an empty dict, so initialize it on the first step. Do the whole update under @torch.no_grad() with in-place ops (mul_, add_, addcmul_, addcdiv_).",
  "Adam: m = b1*m + (1-b1)*g and v = b2*v + (1-b2)*g^2, then correct both by (1 - beta**step) and update p -= lr * m_hat / (sqrt(v_hat) + eps).",
]

[notebooks]
question = "adam.ipynb"
solution = "adam_SOLN.ipynb"

[grading]
tier = "A"        # A=auto-graded B=partial C=not auto-graded (SEEDED, confirm)
entries = ["MyAdam"]
extras = []
device = "cpu"

[[companies]]
name = "Meta"
confidence = "inferred"   # inferred | reported

[[companies]]
name = "Google"
confidence = "inferred"   # inferred | reported
