id = "top-k-sampling"
legacy_ids = ["v3-8"]
title = "Implement Top-k Sampling"
description = "Select the k highest-probability tokens, zero out the rest, renormalize, and sample for controlled text generation."
difficulty = "medium"
frequency = "very-likely"   # very-likely | common | emerging (derived from company reports)
number = 8
set = "v3"
category = "llm-decoding"
tracks = ["llm-path"]

# GENERATED from the grader spec — edit python/torchleet/problems/*.py, then run: generate.py hints
hints = [
  "Keep the k largest logits and make the rest impossible to select.",
  "Setting the discarded logits to -inf before the softmax does that cleanly.",
  "torch.topk gives you both the values and their indices.",
]

[notebooks]
question = "top-k-sampling.ipynb"
solution = "top-k-sampling_SOLN.ipynb"

[grading]
tier = "A"        # A=auto-graded B=partial C=not auto-graded (SEEDED, confirm)
entries = ["top_k_sample"]
extras = []
device = "cpu"

[llm_path]
order = 18
stage = "decoding-inference"

[[companies]]
name = "Anthropic"
confidence = "inferred"   # inferred | reported

[[companies]]
name = "OpenAI"
confidence = "inferred"   # inferred | reported

[[companies]]
name = "DeepMind"
confidence = "inferred"   # inferred | reported

[[companies]]
name = "Cohere"
confidence = "inferred"   # inferred | reported
